import 'package:flutter/material.dart';

class TabAboutScreen extends StatelessWidget {
  const TabAboutScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          Text('add some widgets here',),

        ],
      ),
    );
  }
}
