import 'package:flutter/material.dart';

class TabJobsScreen extends StatelessWidget {
  const TabJobsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          Text('add some widgets here',),

        ],
      ),
    );
  }
}
